# Day별 촬영 가이드

운영 웹을 만들면서 찍을 것. 만드는 내내 이 문서를 옆에 띄워둔다.

핵심은 이거다. **대부분의 화면은 한 번 지나가면 다시 못 본다.** 클러스터는 한 번만 만든다.
`create-next-app` 선택지는 프로젝트당 한 번 뜨고, Vercel Import 화면은 연결하면 끝이다.
찍을 타이밍을 놓치면 계정을 새로 파야 한다.

---

## 0. 촬영 원칙

**키를 가려라.** 이 작업에서 가장 위험한 부분이다. Atlas 연결 문자열, Cloudinary Secret,
구글 OAuth Secret이 그대로 찍힌다. 커리큘럼 Day 14가 다루는 그 사고를 운영자가 먼저 낼 수 있다.

- 스샷 찍고 **바로** 모자이크. "나중에"는 없다
- 가려도 실수했다 싶으면 **그 키는 재발급한다.** 지우는 걸로는 부족하다
- 가장 안전한 건 **교보재용 더미 계정**을 따로 파서 찍는 것

**기타**

- 브라우저 창 너비를 1280px 정도로 고정한다. 스샷 크기가 들쭉날쭉하면 읽기 나쁘다
- 에러 메시지는 **전문이 보이게** 찍는다. 잘린 에러는 검색이 안 된다
- 파일명은 `day07-atlas-connect.png`처럼. 나중에 `#공지`에 붙일 때 찾기 쉽다
- 한글 OS/영문 OS가 다르게 보이는 화면은 둘 다 있으면 좋다 (참가자 OS가 섞인다)

**표기**

- 📸 = 지금 아니면 다시 못 찍는다
- 🔁 = 언제든 재현 가능
- 💥 = 일부러 고장 내서 찍는다. **이게 가장 값진 교보재다**

---

## 계정 만들기 (Day 0 대비)

참가자가 첫날을 통째로 날리는 지점. `curriculum/day-00-setup.md`의 "막히면" 표가 전부 여기서 나온다.

📸 **Orca** — 다운로드 페이지에서 내 OS 빌드를 고르는 화면, 그리고 첫 실행 시
   **홈 디렉토리 접근 권한**을 묻는 화면
📸 **에이전트 콤보박스** — 워크트리 터미널 위에서 에이전트를 고르는 화면.
   Orca 는 에이전트를 주지 않으므로 참가자가 뭘 골라야 하는지 이 한 장이 설명한다
💥 **Windows "PC를 보호했습니다" 경고** — `추가 정보` → `실행` 누르기 전 화면.
   매 기수 몇 명씩 여기서 멈춘다
💥 **Mac "확인되지 않은 개발자"** — 시스템 설정 경로까지

📸 **Vercel 가입 화면** — **"Continue with GitHub" 버튼에 표시**를 해둔다.
   `operator-guide.md`가 "여기서 제일 많이 실수한다"고 꼽은 곳이다. 이메일 가입하면 배포 연동이 꼬인다

📸 **Atlas 클러스터 생성** — 클러스터당 한 번뿐이다. 4장 필요하다
1. `Create` → **M0 (Free)** 선택 화면 (유료 티어와 나란히 보이게)
2. 지역 선택 (`Seoul`)
3. **Database Access** 사용자 생성 — 비밀번호는 다시 못 본다는 경고까지
4. **Network Access** → `0.0.0.0/0` 입력 화면

📸 **Cloudinary Dashboard** — `Cloud name` / `API Key` / `API Secret` 위치에 화살표.
   **값은 전부 가린다.** 위치만 알려주면 된다

📸 **포트원** — 상점 생성 → 결제 연동 → 테스트 채널 추가 → PG사 `KG이니시스` 선택.
   콘솔 UI가 자주 바뀌니 **날짜를 스샷에 같이 남긴다.** 다음 기수엔 안 맞을 수 있다

---

## Day 3 — 첫 배포

전 기수 통틀어 가장 많이 터지는 문제가 여기 있다.

📸 Vercel `Add New` → `Project` → 저장소 `Import` 화면 (프로젝트당 한 번)
📸 빌드 로그가 도는 화면과 **성공 직후 주소가 뜬 화면**
🔁 Source Control에서 커밋 → 푸시 → 1분 뒤 배포 주소가 바뀌어 있는 흐름

💥 **대소문자 사고** — 이 스터디 최대 병목이다. 반드시 재현해서 찍는다
```
style.css → Style.css 로 바꿔 푸시
→ 내 컴퓨터: 멀쩡
→ 배포 사이트: 깨짐 + Network 탭 404
```
   **두 화면을 나란히** 찍어야 교보재가 된다. 찍고 나서 되돌린다

💥 viewport 메타 태그를 지우고 **진짜 핸드폰**으로 접속한 화면.
   F12 반응형 모드는 이 차이가 안 보인다. 폰으로 찍어야 한다

---

## Day 5 — Next.js 이사

📸 **`create-next-app` 선택지 화면** ← `operator-guide.md`가 명시적으로 요구하는 스샷이다.
   TypeScript No / ESLint Yes / Tailwind No / `src/` No / App Router Yes / Turbopack Yes / alias No

> 이건 프로젝트 생성 시 딱 한 번 뜬다. 놓쳤으면 `dulkkot-test` 같은 폴더에
> 한 번 더 만들어서 찍고 지운다. 30초면 된다

📸 **서버 컴포넌트 `console.log` 실험** — 브라우저 콘솔과 터미널을 **한 화면에 나란히**.
   브라우저엔 없고 터미널엔 찍힌다. 2주차 전체의 기반이 되는 장면이라 참가자 인증에서도 요구한다

💥 `'use client'`를 지웠을 때 / 서버 컴포넌트에 `useState`를 넣었을 때 에러 메시지 각각

---

## Day 6–7 — API와 DB

여기가 개념 절벽 1이다. 이탈이 나온다면 여기다.

🔁 `/api/submissions`를 **주소창으로 직접 열어** JSON만 나오는 화면
📸 Atlas `Connect` → `Drivers` → 연결 문자열 화면 (**비밀번호 부분 가리기**)
🔁 Network 탭에서 요청 하나의 Headers / Payload / Response 세 탭

💥 **"로컬은 되는데 배포가 안 돼요" 2위 원인** — Vercel 환경변수를 빼고 재배포.
   로컬 정상 화면 + 배포 에러 화면 **나란히**

💥 비밀번호에 `@` 같은 특수문자를 넣었을 때 `Authentication failed` 터미널 화면
💥 Atlas Network Access에서 `0.0.0.0/0`을 지우고 배포 사이트 접속 → 타임아웃.
   **찍고 반드시 되돌린다**

---

## Day 8–9 — 검증과 업로드

💥 **콘솔에서 `fetch`로 폼을 우회하는 화면** — 이 스터디의 사고방식 자체다
```
폼에 min="0"을 걸어둔 화면
→ 콘솔에서 price: -50000 을 직접 전송
→ 201이 오고 Atlas에 음수가 저장된 화면
```
   **세 장을 한 세트로.** Day 13 결제 금액 조작의 예고편이다

📸 업로드 3단계 요청의 **크기 비교** (Network 탭) — 큰 파일이 우리 서버를 안 거친다는 게 숫자로 보인다
```
/api/upload/sign        작다
api.cloudinary.com/...  크다  ← 실제 파일
/api/submissions        작다  ← URL만
```

💥 **`NEXT_PUBLIC_` 노출 실험** — `operator-guide.md`가 "인상이 가장 강한 실험 중 하나"로 꼽은 것
```
NEXT_PUBLIC_TEST_SECRET=이건비밀이었다
→ Sources 탭 Ctrl+Shift+F 로 검색 → 그대로 나오는 화면
```
   **검색 결과가 하이라이트된 상태**로 찍는다. 이 한 장이 Day 14를 절반 설명한다

---

## Day 10 — 로그인 (최대 병목)

`operator-guide.md`: "구글 콘솔 UI가 복잡하다. 스샷 가이드를 미리 만들어두면 질문이 확 준다."
가장 공들여 찍을 구간이다.

📸 구글 클라우드 콘솔 — 5장이 필요하다
1. 프로젝트 생성
2. `OAuth 동의 화면` 설정 (외부 / 앱 이름 / 이메일)
3. `사용자 인증 정보` → `OAuth 클라이언트 ID` → 웹 애플리케이션
4. **승인된 리디렉션 URI 입력 화면** — localhost와 배포 주소 **둘 다** 들어간 상태
5. 클라이언트 ID / Secret 발급 화면 (**값 가리기**)

💥 **`redirect_uri_mismatch`** — 최대 병목의 실물. 리디렉션 URI를 하나만 등록하고
   다른 쪽에서 로그인을 시도해 에러 화면을 찍는다. **끝 슬래시까지 정확해야 한다**는 걸 같이 보여준다

📸 Application 탭 → Cookies → 세션 쿠키의 `HttpOnly` 체크 표시
🔁 콘솔에 `document.cookie` 쳤을 때 세션 토큰이 **안 나오는** 화면

💥 **권한 우회** — 로그아웃 상태에서 콘솔로 관리자 API 직접 호출
```
401이 오는 화면  = 방어 성공
201이 오는 화면  = 뚫린 것
```
   운영자 본인 앱에서 한 번 뚫려보는 게 가장 좋은 교보재다. 뚫렸으면 그것도 찍어둔다

---

## Day 14–15 — 점검과 측정

📸 Sources `Ctrl+Shift+F`로 `mongodb+srv`, `secret` 검색 → **아무것도 안 나오는 정상 화면**
📸 (있다면) 실제로 뭔가 나온 사고 화면 — 이게 진짜 교보재다
📸 Vercel → Logs에서 실제 요청 하나를 펼친 화면. 배포 환경엔 터미널이 없다는 걸 보여준다

💥 API에서 일부러 에러를 던지고 **사용자 화면 / Vercel 로그**를 나란히.
   사용자에겐 일반 메시지만, 로그엔 전문. 이 비대칭이 정상이다

📸 Lighthouse Before / After 두 장
📸 카톡으로 링크 보냈을 때 미리보기 카드가 뜬 화면

---

## Day 12–13 — 결제 (별도 프로젝트 필요)

이 앱엔 결제가 없다. 스샷을 얻으려면 `curriculum/week-3.md`대로 들꽃 스토어를 따로 만들어야 한다.
이틀치지만 **교보재가 가장 필요한 구간**이라 권한다.

찍어야 할 것

📸 포트원 콘솔 — 테스트 채널의 `storeId` / `channelKey` / `V2 API Secret` 위치 (**값 가리기**)
📸 결제창이 뜬 화면 — **주소가 우리 도메인이 아닌 것**이 보이게
📸 결제 성공 직후 Atlas 주문 문서가 **아직 `PENDING`인** 화면 ← Day 12의 결정적 관찰
📸 Vercel 로그에 웹훅이 들어와 있는 화면 (내가 부른 적 없는 요청)

💥 **금액 조작** — 이 스터디의 하이라이트
```
36,000원 주문 생성 (DB에 36000 기록)
→ 결제창에는 100원으로
→ 100원 결제
→ /api/payments/complete 호출
```
   서버가 거부하고 `PENDING`으로 남은 화면 / 뚫려서 `PAID`가 된 화면. **둘 다 있으면 최고다**

💥 서명 없이 웹훅 POST → `401`이 오는 화면

---

## 랜딩 페이지 2장

공개 랜딩(`/`)에 완성물 사진 자리를 비워뒀다. 찍어서 `study-web/public/shots/`에 넣고
`study-web/app/page.js`의 `SHOTS` 상수에 경로를 채우면 켜진다. `null`이면 그 자리는
빈 상자 없이 접히므로 **급하지 않다.**

📸 `hero.png` — 완성된 들꽃 스토어 상품 목록. 1600×1200, 데스크톱
📸 `build.png` — 관리자 상품 등록 화면, 이미지 업로드가 도는 중. 1400×1050

모집용이라 **깨진 화면이 아니라 잘 돌아가는 화면**을 찍는다. 이 두 장만 이 문서에서
성격이 다르다. 나머지는 전부 가르치려고 찍는 것이고, 이건 팔려고 찍는 것이다.

---

## 전체 체크리스트 62장

앞의 Day별 설명이 왜 찍는지를 말한다면, 이 표는 **무엇이 남았는지**를 말한다.
찍은 것은 `☐`를 `☑`로 바꾼다.

📌 = 커리큘럼 본문에 자리표시가 이미 잡혀 있다. 파일명이 정해져 있으니 그대로 써야 한다
(`curriculum/images/` 12장, 자세한 건 그 폴더의 README).

### A. 계정 만들 때 한 번뿐 · 14장

**지나가면 계정을 새로 파야 한다.** Day 0 준비가 곧 촬영이다. 지금이 이 구간이다.

| | 장면 | 파일명 |
|---|---|---|
| ☐ | Orca 다운로드 — 내 OS 빌드 고르는 화면 | 📌 `day00-orca-download.png` |
| ☐ | Orca 첫 실행 — 홈 디렉토리 접근 권한 요청 | |
| ☐ | Vercel 가입 — **Continue with GitHub 버튼에 표시** | |
| ☐ | Atlas — `Create` → **M0 (Free)** (유료와 나란히) | |
| ☐ | Atlas — 지역 `Seoul` | |
| ☐ | Atlas — Database Access 사용자 생성 (비번 경고까지) | |
| ☐ | Atlas — Network Access `0.0.0.0/0` | |
| ☐ | Cloudinary — 세 값 **위치**에 화살표 (값은 가림) | |
| ☐ | 포트원 — 테스트 채널 + `KG이니시스` 선택 | |
| ☐ | 구글 — 프로젝트 생성 | |
| ☐ | 구글 — OAuth 동의 화면 (외부/앱 이름/이메일) | |
| ☐ | 구글 — OAuth 클라이언트 ID → 웹 애플리케이션 | |
| ☐ | 구글 — **리디렉션 URI에 localhost와 배포 주소 둘 다** | 📌 `day10-redirect-uri.png` |
| ☐ | 구글 — 클라이언트 ID / Secret 발급 (값 가림) | |

### B. 프로젝트 만들 때 한 번뿐 · 8장

다시 찍으려면 빈 프로젝트를 하나 더 만들어야 한다. 30초면 되니 놓쳐도 복구는 된다.

| | 장면 | 파일명 |
|---|---|---|
| ☐ | Orca — 저장소 추가 후 워크트리가 열린 사이드바 | 📌 `day00-orca-addrepo.png` |
| ☐ | Orca — 워크트리 터미널 위 **에이전트 콤보박스** | |
| ☐ | Vercel — `Add New` → `Project` → Import | 📌 `day03-vercel-import.png` |
| ☐ | Vercel — 빌드 로그가 도는 중 | |
| ☐ | Vercel — 배포 성공 직후 주소가 뜬 화면 | 📌 `day03-deploy-done.png` |
| ☐ | `create-next-app` — **선택지 세 개가 다 보이게** | 📌 `day05-create-next-app.png` |
| ☐ | Atlas — `Connect` → `Drivers` 연결 문자열 (비번 가림) | 📌 `day07-atlas-connect.png` |
| ☐ | 포트원 — `storeId` / `channelKey` / `V2 Secret` 위치 | |

### C. 언제든 재현 가능 · 40장

**급하지 않다.** 여유 있을 때 작정하고 몰아 찍는 편이 낫다.
특히 💥는 전부 되돌릴 수 있으니 마음 편히 부순다.

| | 장면 | 파일명 |
|---|---|---|
| 🔁 ☐ | Orca diff — **거터의 AI 마커** | 📌 `day01-orca-diff.png` |
| 🔁 ☐ | Styles 패널 — **진 규칙에 취소선** | 📌 `day02-styles-strikethrough.png` |
| 🔁 ☐ | 커밋 → 푸시 → 1분 뒤 배포가 바뀐 흐름 | |
| 🔁 ☐ | `/api/submissions`를 주소창으로 직접 열어 JSON만 | |
| 🔁 ☐ | Network 탭 — Headers / Payload / Response | |
| 🔁 ☐ | 콘솔 `document.cookie`에 세션이 **안 나오는** 화면 | |
| 📸 ☐ | 업로드 3단계 요청의 **Size 열 차이** | 📌 `day09-upload-network.png` |
| 📸 ☐ | 서버 컴포넌트 `console.log` — 브라우저와 터미널 나란히 | |
| 📸 ☐ | Application → Cookies → `HttpOnly` 체크 | |
| 📸 ☐ | Sources 검색 → **아무것도 안 나오는 정상 화면** | |
| 📸 ☐ | Vercel Logs — 요청 하나를 펼친 화면 | |
| 📸 ☐ | Lighthouse Before / After (2장) | |
| 📸 ☐ | 카톡 링크 미리보기 카드 | |
| 📸 ☐ | 결제창 — **주소가 우리 도메인이 아닌 것**이 보이게 | |
| 📸 ☐ | 결제 성공 직후 주문이 **아직 `PENDING`인** 화면 | |
| 📸 ☐ | Vercel 로그에 웹훅이 들어와 있는 화면 | |
| 💥 ☐ | **대소문자 사고** — 로컬·배포 나란히 (2장) | 📌 `day03-case-sensitivity.png` |
| 💥 ☐ | viewport 메타 삭제 + **진짜 핸드폰**으로 접속 | |
| 💥 ☐ | `'use client'` 삭제 / 서버에 `useState` 에러 (2장) | |
| 💥 ☐ | Vercel 환경변수 빼고 재배포 — 로컬·배포 나란히 (2장) | |
| 💥 ☐ | 비번에 `@` → `Authentication failed` | |
| 💥 ☐ | Network Access `0.0.0.0/0` 삭제 → 타임아웃 | |
| 💥 ☐ | 콘솔 `fetch`로 폼 우회 — 폼 / 전송 / 저장된 음수 (3장) | |
| 💥 ☐ | `NEXT_PUBLIC_` 노출 — 검색 결과가 하이라이트된 채로 | |
| 💥 ☐ | `redirect_uri_mismatch` 에러 화면 | |
| 💥 ☐ | 권한 우회 — `401` / `201` (2장) | |
| 💥 ☐ | 에러 비대칭 — 사용자 화면 / Vercel 로그 (2장) | |
| 💥 ☐ | **금액 조작** — 결제창 100원 / DB 36000원 (2장) | 📌 `day13-amount-tamper.png` |
| 💥 ☐ | 서명 없이 웹훅 POST → `401` | |

### 랜딩 · 2장

| | 장면 | 파일명 |
|---|---|---|
| ☐ | 완성된 들꽃 스토어 상품 목록 | `public/shots/hero.png` |
| ☐ | 관리자 상품 등록 (업로드 중) | `public/shots/build.png` |

---

## 지금 당장 할 것

Day 0이 이틀 뒤다. **A 구간 14장이 전부 이번 주에 지나간다.**
계정을 만들기 전에 이 문서를 열어두고, 화면이 뜰 때마다 찍는다.

B와 C는 만들면서 자연스럽게 나온다. C는 급하지 않다.

Day 12–13 결제 6장은 이 앱에 결제가 없어서 **들꽃 스토어를 따로 만들어야** 나온다.
교보재가 가장 필요한 구간이지만 이틀치 작업이라, 1주차를 굴리면서 짬을 내는 편이 현실적이다.
